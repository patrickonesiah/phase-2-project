import React from 'react';

function Middle({storyTitle, storyDescription}) {

    return(
        <>
            <h1>{storyTitle}</h1>
            <p>{storyDescription}</p>
        </>
    )
}

export default Middle;